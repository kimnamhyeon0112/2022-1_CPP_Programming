#include "Homework10.h"
#include <string>
using namespace std;

int main()
{
	if (Select == 1)
	{
		Point m = { 10,20 };
		double l = m;
		cout << "결과: " << l << endl;
		cout << m << endl << endl;
	}
	else if (Select == 2)
	{
		Point d = { 20,10 };
		double l = d;
		cout << "결과: " << l << endl;
		cout << d << endl << endl;
	}
	else if (Select == 3)
	{
		Point o = { 10,20 };
		double l = o;
		cout << "결과: " << l << endl;
		cout << o << endl << endl;
	}
	else if (Select == 4)
	{
		Point s = { 10,20 };
		Point s3 = (s * s);
		cout << "결과: " << s3 << endl;
		cout << s << endl << endl;
	}
	else if (Select == 5)
	{
		Point m = { 10,20 };
		double l = m;
		cout << "결과: " << l << endl;
		cout << m << endl << endl;
	}
	else if (Select == 6)
	{
		Point l = { 10,20 };
		double le = l;
		cout << "결과: " << le << endl;
		cout << l << endl << endl;
	}
	else if (Select == 7)
	{
		Point em = { 10,20 };
		double l = em;
		cout << "결과: " << l << endl;
		cout << em << endl << endl;
	}
	else if (Select == 8)
	{
		Point el = { 10,20 };
		double l = el;
		cout << "결과: " << l << endl;
		cout << el << endl << endl;
	}
	else if (Select == 9)
	{
		Point e = { 10,20 };
		double l = e;
		cout << "결과: " << l << endl;
		cout << e << endl << endl;
	}
	else if (Select == 10)
	{
		Point ne = { 10,20 };
		double l = ne;
		cout << "결과: " << l << endl;
		cout << ne << endl << endl;
	}
	return 0;
}